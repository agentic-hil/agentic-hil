# Build and SBOM tooling for the release attestation job
# (.github/workflows/workflow.yml).
#
# Regenerate the hash-pinned lock file after editing, or run
# `python tools/refresh_locks.py --regenerate`, which runs the command every lock records:
#   uv pip compile --generate-hashes --universal requirements/sbom.in -o requirements/sbom.txt
build
cyclonedx-bom
