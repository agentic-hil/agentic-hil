# Generated firmware build output stays local.
build/
!.settings

# Agentic HIL config is host-specific: OpenOCD paths, COM devices, reports, and logs
# belong to the local machine running the hardware loop.
/.agentic-hil
/.vscode
