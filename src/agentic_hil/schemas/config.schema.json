{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentic-hil.local/schemas/config.schema.json",
  "title": "Agentic HIL configuration",
  "description": "Authoritative Agentic HIL project configuration containing all hardware resources, permissions, and limits. Reading a device needs no permission from version 2 on: exclusivity for the duration of a run replaced the read permission, so allow_probe and allow_read exist only in version 1 files. Writing and state-changing operations stay deny-by-default and are declared per device: on the debugger that may be flashed or reset, and on each COM port and CAN bus that may be written.",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "debugger_permissions": {
      "type": "object",
      "description": "What this debug probe may do beyond reading it. Deny-by-default; scoped to this debugger alone. allow_probe is a version 1 field and is refused in a version 2 file.",
      "additionalProperties": false,
      "properties": {
        "allow_probe": {
          "type": "boolean",
          "default": false,
          "deprecated": true,
          "description": "Version 1 only. Reading a target needs no permission from version 2 on; remove the key and bump version."
        },
        "allow_flash": {
          "type": "boolean",
          "default": false
        },
        "allow_reset": {
          "type": "boolean",
          "default": false
        },
        "allow_raw_debugger_commands": {
          "type": "boolean",
          "default": false
        },
        "allow_mass_erase": {
          "type": "boolean",
          "default": false
        }
      },
      "default": {}
    },
    "io_permissions": {
      "type": "object",
      "description": "What this COM port or CAN bus may do beyond reading it. Deny-by-default; scoped to this device alone. allow_read is a version 1 field and is refused in a version 2 file.",
      "additionalProperties": false,
      "properties": {
        "allow_read": {
          "type": "boolean",
          "default": false,
          "deprecated": true,
          "description": "Version 1 only. Reading a device needs no permission from version 2 on; remove the key and bump version."
        },
        "allow_write": {
          "type": "boolean",
          "default": false
        }
      },
      "default": {}
    }
  },
  "required": [
    "workspace_root",
    "state_root"
  ],
  "properties": {
    "version": {
      "type": "integer",
      "enum": [
        1,
        2
      ],
      "default": 1,
      "description": "Which permission model this file is read under. Absent means version 1: reading is gated by allow_probe and allow_read, which is how every file written before this release behaves and keeps behaving. Version 2 has no read permission at all; migrating means removing those keys and setting version: 2 in one edit."
    },
    "workspace_root": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path of the workspace this configuration authorizes. It is the runtime working directory."
    },
    "state_root": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute operator-controlled directory shared by every frontend for persistent locks, leases, quarantine incidents, and canonical reports. It must be outside workspace_root."
    },
    "permissions": {
      "type": "object",
      "description": "Project-scoped permissions: what may be done to this configuration itself. Deny-by-default like every other write permission in this file, and like those only ever widened by a human editing this file. Three grants rather than one, so that opening the file far enough for an agent to enter a probe serial does not also hand over the granting of allow_flash.",
      "additionalProperties": false,
      "properties": {
        "allow_config_write": {
          "type": "boolean",
          "default": false,
          "description": "Whether an agent may regenerate this whole configuration from hardware discovery through project_config_create. False in every configuration an agent generated for itself, which is what makes agent provisioning one-time: an agent can enable itself up to observation, never up to modification. Set it true by hand to let the agent regenerate this file afterwards; even then the agent supplies no permission value, only the discovered hardware facts."
        },
        "allow_config_description_write": {
          "type": "boolean",
          "default": false,
          "description": "Whether an agent may change this bench's description field-wise through project_config_set: target.*, debuggers.<name>.probe_id/executable/interface_cfg/target_cfg, com_ports.<name>.device/baudrate, and every can_buses.<name> field except its permissions. It never reaches a permissions: block, so granting it does not let the agent widen what it may do to the hardware. This is the grant for the common case: a board was attached after init and its identity has to be entered."
        },
        "allow_config_permissions_write": {
          "type": "boolean",
          "default": false,
          "description": "Whether an agent may change the permissions: blocks field-wise through project_config_set, including this key and allow_config_description_write. This is the grant that hands over the granting: with it, an agent can set allow_flash and allow_mass_erase on itself. Leave it false unless that is deliberately what you want."
        }
      },
      "default": {}
    },
    "provenance": {
      "type": "object",
      "description": "Who wrote this file and who last changed it. Informational only: nothing reads it as policy and it grants nothing. It exists so a later reader does not have to guess whether a person intended these contents.",
      "additionalProperties": false,
      "properties": {
        "created_by": {
          "type": "string",
          "enum": [
            "agent",
            "human"
          ],
          "description": "\"agent\" means an agent generated this file over MCP out of hardware discovery, with every permission false."
        },
        "created_via": {
          "type": "string",
          "description": "The interface that wrote it, e.g. mcp:project_config_create."
        },
        "created_at": {
          "type": "string",
          "description": "UTC ISO-8601 timestamp of the write."
        },
        "agentic_hil_version": {
          "type": "string"
        },
        "last_modified_by": {
          "type": "string",
          "enum": [
            "agent",
            "human"
          ],
          "description": "Who made the most recent field-wise change. Written by project_config_set; a person editing this file by hand leaves it as it was, which is itself the signal that the recorded change is not the newest one."
        },
        "last_modified_via": {
          "type": "string",
          "description": "The call that made the most recent change, e.g. mcp:project_config_set."
        },
        "last_modified_at": {
          "type": "string",
          "description": "UTC ISO-8601 timestamp of the most recent field-wise change."
        },
        "last_modified_keys": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The dotted keys the most recent field-wise change set, so a later reader sees what moved and not only that something did."
        },
        "modification_count": {
          "type": "integer",
          "minimum": 0,
          "description": "How many field-wise changes have been applied to this file. A count rather than a log: an unbounded history inside the policy file itself would grow without limit, and the count is what tells a reader whether last_modified_keys is the whole story."
        }
      }
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "default": "unknown-target"
        },
        "controller": {
          "type": "string",
          "default": "unknown-controller"
        }
      }
    },
    "debuggers": {
      "type": "object",
      "description": "Named debug probes. The name is the routing key: test plan steps address a probe by it, and each probe carries its own permissions. Once several are configured every entry needs its own probe_id — it is the only field that selects a physical probe, and resource_id merely aliases the coordination lease, so it never substitutes for one.",
      "propertyNames": {
        "pattern": "^[A-Za-z0-9_.-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "type": {
            "type": "string",
            "default": "openocd",
            "enum": [
              "openocd",
              "stlink",
              "pyocd"
            ]
          },
          "executable": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "probe_id": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9_.:-]+$",
            "default": null
          },
          "target_type": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9_.-]+$",
            "default": null
          },
          "interface": {
            "type": "string",
            "default": "SWD",
            "enum": [
              "SWD",
              "JTAG"
            ]
          },
          "interface_cfg": {
            "type": "string",
            "default": "interface/stlink.cfg"
          },
          "target_cfg": {
            "type": "string",
            "default": "target/stm32f4x.cfg"
          },
          "flash_address": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^(0x[0-9A-Fa-f]+|[0-9]+)$",
            "default": null
          },
          "resource_id": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9_.:-]+$",
            "default": null
          },
          "timeout_s": {
            "type": "number",
            "minimum": 0,
            "default": 60.0
          },
          "permissions": {
            "$ref": "#/$defs/debugger_permissions"
          },
          "target": {
            "type": "object",
            "additionalProperties": false,
            "description": "Per-debugger target override for a probe on a second board; unspecified fields default to the top-level target.",
            "properties": {
              "name": {
                "type": "string"
              },
              "controller": {
                "type": "string"
              }
            }
          }
        }
      },
      "default": {}
    },
    "debug": {
      "type": "object",
      "description": "Typed debug-session settings for safe GDB/OpenOCD operations.",
      "additionalProperties": false,
      "properties": {
        "gdb_executable": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "allowed_symbols": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*(?:::[A-Za-z_][A-Za-z0-9_]*)*$"
          },
          "default": []
        },
        "allow_all_symbols": {
          "type": "boolean",
          "default": false
        },
        "max_dump_size_bytes": {
          "type": "integer",
          "minimum": 1,
          "default": 1048576
        }
      },
      "default": {}
    },
    "artifacts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "allowed_roots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "build"
          ]
        },
        "upload_directory": {
          "type": "string",
          "default": ".agentic-hil/artifacts"
        },
        "allowed_extensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            ".elf",
            ".hex",
            ".bin"
          ]
        },
        "max_upload_size_mb": {
          "type": "integer",
          "minimum": 1,
          "default": 64
        },
        "allow_upload": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "com_ports": {
      "type": "object",
      "description": "Named serial/COM ports that Agentic HIL may open for stimuli and feedback. Reading needs no permission; assert_dtr and assert_rts decide whether opening the port touches the target.",
      "propertyNames": {
        "pattern": "^[A-Za-z0-9_.-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "device"
        ],
        "properties": {
          "device": {
            "type": "string"
          },
          "baudrate": {
            "type": "integer",
            "minimum": 1,
            "default": 115200
          },
          "timeout_s": {
            "type": "number",
            "minimum": 0,
            "default": 0.1
          },
          "write_timeout_s": {
            "type": "number",
            "minimum": 0,
            "default": 1.0
          },
          "encoding": {
            "type": "string",
            "default": "utf-8"
          },
          "assert_dtr": {
            "type": "boolean",
            "default": true,
            "description": "Whether DTR is asserted when the port is opened. pyserial asserts it by default, and on boards that wire DTR to reset that restarts the target on every open. Set false to observe a target provably undisturbed; leave true when the peer needs DTR to talk."
          },
          "assert_rts": {
            "type": "boolean",
            "default": true,
            "description": "Whether RTS is asserted when the port is opened. Same reasoning as assert_dtr; some boards wire RTS to reset or boot mode."
          },
          "max_buffer_bytes": {
            "type": "integer",
            "minimum": 1,
            "default": 65536
          },
          "max_write_bytes": {
            "type": "integer",
            "minimum": 1,
            "default": 4096
          },
          "resource_id": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9_.:-]+$",
            "default": null
          },
          "permissions": {
            "$ref": "#/$defs/io_permissions"
          }
        }
      },
      "default": {}
    },
    "can_buses": {
      "type": "object",
      "description": "Named CAN buses that Agentic HIL may open for stimuli and feedback.",
      "propertyNames": {
        "pattern": "^[A-Za-z0-9_.-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "channel"
        ],
        "properties": {
          "adapter": {
            "type": "string",
            "default": "peak",
            "enum": [
              "peak",
              "socketcan",
              "process"
            ]
          },
          "channel": {
            "type": "string"
          },
          "bitrate": {
            "type": "integer",
            "minimum": 1,
            "default": 500000
          },
          "fd": {
            "type": "boolean",
            "default": false
          },
          "data_bitrate": {
            "type": [
              "integer",
              "null"
            ],
            "minimum": 1,
            "default": null
          },
          "pcanbasic_dll": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "executable": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "timeout_s": {
            "type": "number",
            "minimum": 0,
            "default": 10.0
          },
          "poll_interval_ms": {
            "type": "integer",
            "minimum": 1,
            "default": 10
          },
          "receive_own_messages": {
            "type": "boolean",
            "default": false
          },
          "listen_only": {
            "type": "boolean",
            "default": false
          },
          "max_buffer_frames": {
            "type": "integer",
            "minimum": 1,
            "default": 1024
          },
          "max_frame_data_bytes": {
            "type": "integer",
            "minimum": 0,
            "maximum": 64,
            "description": "Maximum CAN payload size in bytes. Defaults to 8, or 64 when fd is true."
          },
          "resource_id": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^[A-Za-z0-9_.:-]+$",
            "default": null
          },
          "permissions": {
            "$ref": "#/$defs/io_permissions"
          }
        }
      },
      "default": {}
    },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "require_existing_file": {
          "type": "boolean",
          "default": true
        },
        "require_allowed_root": {
          "type": "boolean",
          "default": true
        },
        "require_allowed_extension": {
          "type": "boolean",
          "default": true
        },
        "compute_sha256": {
          "type": "boolean",
          "default": true
        },
        "inspect_known_formats": {
          "type": "boolean",
          "default": true
        }
      }
    },
    "reports": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "directory": {
          "type": "string",
          "default": ".agentic-hil/reports"
        }
      }
    },
    "logs": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "directory": {
          "type": "string",
          "default": ".agentic-hil/logs"
        }
      }
    },
    "recovery": {
      "type": "object",
      "description": "How far the owning process may clear its own hardware quarantines before an operator is required.",
      "additionalProperties": false,
      "properties": {
        "auto_recover": {
          "type": "string",
          "enum": [
            "off",
            "readonly",
            "reset_halt"
          ],
          "default": "reset_halt",
          "description": "off: only an operator clears a quarantine. readonly: reap this owner's debugger processes and re-read the probe, touching nothing physical. reset_halt: additionally drive a reset-into-halt to settle an unconfirmed flash or reset. Use readonly on a bench whose peripherals react to a reset."
        },
        "max_attempts": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "default": 3,
          "description": "Machine recovery attempts allowed per quarantine incident before the owner stops trying and defers to an operator."
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "required": [
          "version"
        ],
        "properties": {
          "version": {
            "const": 2
          }
        }
      },
      "then": {
        "properties": {
          "debuggers": {
            "additionalProperties": {
              "properties": {
                "permissions": {
                  "properties": {
                    "allow_probe": false
                  }
                }
              }
            }
          },
          "com_ports": {
            "additionalProperties": {
              "properties": {
                "permissions": {
                  "properties": {
                    "allow_read": false
                  }
                }
              }
            }
          },
          "can_buses": {
            "additionalProperties": {
              "properties": {
                "permissions": {
                  "properties": {
                    "allow_read": false
                  }
                }
              }
            }
          }
        }
      }
    }
  ]
}
